# 更新日志

本文件记录每个版本改了什么。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [v1.0.0] — 2026-09-07

首次发布。下面按主题列出这一版做的事，括号里是对应的提交。

### 修复：三个会直接导致装不上的硬故障

- **Windows 双击 `install.bat` 窗口一闪即消**。文件是 LF 行尾，而 cmd.exe 解析批处理依赖 CRLF，
  碰到 `if (...)` 这种带括号的多行块会直接中止。已转 CRLF，并改用 `goto` 结构
  （顺带修掉退出码恒为 0 的问题：`%errorlevel%` 在括号块进入前就被展开了），每个退出分支都加了
  `pause`，以后出错窗口不会关，能看到原因。（`a1eacef`）
- **macOS 上 Node 装不上**。原来装的是 `node@22`，它是 keg-only 的，不会 symlink 进 PATH，
  于是紧接着的 `command -v node` 必然失败。改用主 formula `node`。（`a1eacef`）
- **服务永远起不来**。启动路径写的是 `scripts/dev/run-next.mjs`，那是 omniroute 开发仓库里的文件，
  **发布到 npm 的包里根本没有**，`spawn` 拿到空参数，健康检查空等三分钟后退出，配置一个字都没写。
  改用发布包提供的 `bin/omniroute.mjs serve`。（`a1eacef`）

### 修复：安装流程

- Windows 装完没有 `omni` 命令 —— 原来只写 shell alias，而 cmd 和 PowerShell 从不读它。
  现在生成 `omni.cmd` 并追加到用户级 PATH（不用 `setx`，避免它在 PATH 超长时截断）。（`a1eacef`）
- npm 安装的成败改用**退出码**判断，不再看 stdout 是否为空（装成功却被判为失败，白装第二遍），
  同时让 npm 的进度直接显示，不再静默十几分钟。（`a1eacef`）
- `set -e` 下 brew 任何非零返回都会让脚本静默退出，即使 Node 其实装好了。（`7622f76`）
- 关掉 Homebrew 6.0 起默认开启的英文确认提示 `Do you want to proceed...`，
  它只在真实终端出现，管道跑的测试永远看不到。（`83da783`）
- 安装前说清成本（下载 116MB / 装完约 2.3GB / 5–15 分钟），并检查磁盘空间，
  避免写进去 2G 才失败。检查的是安装目录所在分区，不是 macOS 那个只读的 `/`。（`f0de2c0`）

### 新增：macOS 双击入口

- `install.command` / `uninstall.command`。macOS **无法执行双击的 `.sh`**，
  这也是「打开方式」里从来找不到终端的原因。（`a1eacef`）

### 新增：对话体验

- 流式输出，逐行渲染 Markdown（小标题、**粗体**、`代码`、列表、代码块在终端里上色）。
- 多轮上下文 —— 此前每一轮都是孤立发送的，AI 完全记不住上文。
- 思考动画，带已等待秒数；每次回答后显示耗时、实际模型、输入/输出 token。
- **断点接手**：上游中途断流时自动换源，并把已生成的半截交给接手的模型续写，
  不从头重来，模型栏会显示切换轨迹（如 `A → B`）。
- 失败讲人话：把上游错误翻译成「额度已用完，约 2.5 小时后恢复」这类说明，
  恢复时间从 `Retry-After` / `X-RateLimit-Reset` 换算；拿不到就明说未告知，不编造。
- `:q` 与 Ctrl+C 会真正中断进行中的请求，而不是让进程继续等一个可能永远不来的响应。
- 回答风格可调：`config.json` 的 `systemPrompt` 控制「先给结论、分点、简短、最小代码」。

### 新增：本地长记忆

- 每轮对话按天存进 `<目录>/memory/YYYY-MM-DD.md`，并建关键词索引。
- 提问时**只检索并注入最相关的 3 条历史片段**，而不是把全部历史塞进上下文 —— 这是省 token 的关键。
- 关键词用滑窗 n-gram 规则提取，零成本、不额外调用 AI。
- 新增 `omni memory list / search / graph`（graph 输出 mermaid 关系图）。

### 新增：卸载

- 逐项可选，不是一刀切：列出每项的实际体量和是否可恢复，输入编号或 `a` 全删。（`964544b`）
- 选完后**按文件类型**摊开将删除什么（对话记录 / 记忆索引 / 配置 / 数据库 / 日志）。
- 涉及不可恢复内容时，默认提示把 `config.json` 和整个 `memory/` 备份到桌面。
- 覆盖到此前一直遗漏的东西：Node.js 及其 brew 依赖、npm 下载缓存与日志（`964544b`）、
  `~/.npm`、`~/.config/configstore/update-notifier-omniroute.json`、脚本自己留下的
  `.omni-bak`（`f41c40f`）、brew 里 node 卸载后残留的 npm 目录与下载缓存（`d982883`）。
- **只删自己带来的**：删掉自身内容后若文件/目录空了才连它一起删，且必须有证据
  （这次删到了 alias 段，或旁边有我们写的 `.omni-bak`）；里面还有别人的东西就只删自己那部分。（`f4ab91b`）
- Homebrew 本身不在自动删除范围（它的安装时间远早于本工具），只打印官方卸载命令供你决定。

### 修复：一类只有真人用才会碰到的问题

这些都躲过了自动化测试，因为测试是用管道驱动的：

- `read -p` 的提示语**不解释转义序列**，颜色变量用普通单引号定义时会把 `\033[1m` 原样吐给用户，
  于是 `[1ma=一键全删` 看起来像要输入 `1ma`。改用 `$'...'`，并把提示语拆成
  「说明用 `echo -e`、`read` 只留纯文本」。（`38fad0a`）
- 卸载编号只按空格分隔，输 `1,2,3` 会一个都匹配不上，然后**跳过所有操作却照样打印「完成」**。
  现在逗号/顿号/分号都认，识别不出有效编号会明确报错并非零退出，确认前先回显已识别的编号。（`c4818cc`）
- 确认只接受完整的 `yes`，输 `y` 被静默取消，用户会以为是命令输错了。现在会说明原因。（`c505406`）
- `.command` 结尾承诺「按回车键关闭本窗口」，但窗口关不关由终端设置决定，脚本无权控制。
  改成说实话，并给出让它自动关的一次性设置。（`fd79003`）
- 卸载 node 后用 `command -v node` 判断「它还在不在」是错的：bash 会缓存执行过的命令路径，
  文件删了 `command -v` 仍返回旧路径。改为直接检查文件并先 `hash -r`。（`e9e4472`）

### 测试

- `make test` 跑 `tests/regress.sh`，覆盖：语法、行尾与 BOM、可执行位、
  **内嵌模板与生成文件的往返一致性**、mock 上游的功能冒烟、卸载的安全路径与真正执行路径，
  以及上面那几类「只有真人才碰得到」的问题的静态断言。当前 80 项。
- 缺 Node 时自动降级，静态检查照跑，只跳过需要 Node 的部分。

---

## 提交列表

| 提交 | 时间 | 说明 |
|---|---|---|
| `7a91ea4` | 18:48 | README 改为面向非技术读者 |
| `f0de2c0` | 18:23 | 安装前告知成本并检查磁盘空间 |
| `83da783` | 18:03 | 关掉 brew 的英文确认提示 |
| `e9e4472` | 17:57 | 卸载 node 后改用文件检查而非 `command -v` |
| `fd79003` | 17:50 | 不再承诺脚本关不掉的窗口 |
| `7622f76` | 17:09 | brew 非零退出不再静默中断安装 |
| `f4ab91b` | 17:02 | 只删自己创建的文件，保留原本就有的 |
| `d982883` | 16:58 | 清理 brew 里的 node 残留，并修掉劝退用户的菜单文案 |
| `f41c40f` | 16:54 | 补上三处此前遗漏的残留 |
| `38fad0a` | 16:48 | 修复 read 提示语吐出转义序列 |
| `c4818cc` | 16:39 | 无法识别的编号不再被静默接受 |
| `c505406` | 16:33 | 说明为什么输 `y` 会取消 |
| `964544b` | 16:11 | 卸载覆盖 Node、npm 缓存等安装带来的一切 |
| `16e4548` | 16:02 | 公开前移除 README 里的内部备注 |
| `a1eacef` | 16:00 | 修复跨平台安装器并重做对话体验 |
